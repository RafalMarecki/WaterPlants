<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WaterPlants"
        tools:targetApi="31">
        
        <provider
            android:authorities="com.example.fileprovider"
            android:name="androidx.core.content.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/image_provider"/>
        </provider>

        
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name=".activities.MyPlantsActivity"></activity>
        <activity android:name=".activities.PlantDetailsActivity"></activity>
        <activity android:name=".activities.ChoosePlantActivity"></activity>
        <activity android:name=".activities.ChoosePlantDetailsActivity"></activity>
        <activity android:name=".activities.WateringActivity"></activity>
        <activity android:name=".activities.FertilizingActivity"></activity>
        <activity android:name=".activities.ActivityNoResults"></activity>
    </application>

</manifest>